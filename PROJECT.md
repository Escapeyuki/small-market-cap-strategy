# 小市值策略 — 项目笔记

复现德邦证券 2022-04-20《金工小市值专题之一：小市值策略初探》。

- **目标** — 忠实复现（对上 43.1% 是真实的验收标准），工程质量按实盘标准，动机是学习量化金融。
- **论文是基线**，后续会在其上叠加变体。
- **决策纪要** → [`grill.md`](grill.md)。动结构之前先读它——里面写了**为什么**，包括若干原始前提后来被证伪的决策。

---

## 策略本体（一段话）

对全 A 股按**总市值**（`market_cap`，非流通市值）排序，取**最小的 100 只等权持有、月度调仓**。选股范围剔除 ST/\*ST、北交所、上市不满 20 个交易日的次新股；并在**每个信号日**额外剔除当日涨停与停牌的股票。成本按往返 0.3% 计。基准为中证 1000（`000852.XSHG`）。

**成交口径：T 日收盘出信号，T+1 开盘成交**（grill.md Q19）。**研报从未交代它在什么价上成交**——本项目原按「T 日收盘成交」重建它，那是未来函数，已删除。⇒ **本仓库跑出来的不是研报那个数，是「同样的策略、正确执行」的数**：主结果 **43.00%**（研报 43.1%）。

论文口径（2012-12-03 ~ 2022-03-31）：年化 43.1%、夏普 1.28、最大回撤 54.7%。

~~一个值得保留的反直觉结论：日频调仓显著跑输月频。~~ **这条已被 Q19 推翻**——研报的「日频跑输」有相当一部分是它自己的成交口径造成的。换成 T+1 开盘后，日频从 38.0% 变成 51.9%，反而最好。真正的结论是：**日频下「成交口径」这一个选择就值 14pp，压过频率本身的效应，所以研报关于调仓频率的结论不稳健**（两个方向都不稳健，别信 51.9%）。详见 grill.md Q19。

---

## 进度

| 层 | 组件数 | 已完成 |
|---|---|---|
| 数据层 | 4 | ✅ 4 |
| 绩效层 | 1 | ✅ 1 |
| 策略层 | 5 | 🟡 4 |
| 分析层 | 8 | ✅ 8 |
| 输出层 | 2 | 🟡 1 |
| **合计** | **20** | **18** |

**第一次验收已通过：小市值100 月频年化 43.09%，研报 43.1%。** 六项结构性检查
全部成立，含调出次数 2368 vs 2401、最大回撤区间逐日吻合。详见
[`grill.md`](grill.md)「第一次验收」——**包括那句「对到 0.01pp 有运气成分」的
提醒，以及 Q14 的单变量敏感性映射表。**（那次验收跑的是研报口径，该口径此后
已按 Q19 删除；当前口径下的对应数字见 grill.md「Q19 之后的复跑」，**结论一条
都没有翻**。）

**第二次验收（分析层）也已通过**，见 [`grill.md`](grill.md)「第二次验收」。这轮
的证据比头条数字硬得多：**图28 日历效应 15 个数字与研报最大只差 0.02pp**——它
约束的是 2,267 个交易日按星期几的分组均值，也就是整条净值路径的细结构，不是
一个可以被四个自由参数凑出来的端点。Q14 分层验收的最后一项（图26 调出归因）
同时结清：折叠成研报的三类后 97.85 / 2.15 / 0.00，研报 97.17 / 2.75 / 0.08。

**授权到期风险已解除。** rqdatac 是有硬期限的试用授权，抓取已全部完成，此后全程离线跑本地 Parquet。当日消耗 810 MB / **每日** 1 GB 额度（该额度按日重置——最初被误判为终身额度）。

**样本外（Phase 4）已完成。** 同一套策略、参数逐字不变延伸到 2026-08：样本外块年化
**28.3%**（样本内 43.0%），超额 +25.3pp 但**夏普/信息比率腰斩**；**2024-01 微盘踩踏 −50%**
（基准中证1000 仅 −15%，≈3 倍，样本内不存在的独有形态），退市新规后**戴帽调出翻三倍**。
落地 `scripts/05_oos.py`，两条「研报已过时之处」预判都成立。详见 [`grill.md`](grill.md)「样本外回测」。

---

## 目录结构

```
小市值策略/
├── PROJECT.md            # 本文件
├── grill.md              # 决策纪要：18 项决策与理由、两次验收、纪律条款
├── config.yaml           # 全部可调参数：区间、阶段、N、费率、频率、阈值、资金量
├── smallcap/
│   ├── config.py         # 加载 config.yaml，导出 CFG 与 DATA_ROOT
│   ├── data.py           # rqdatac 抓取 + Parquet 缓存 + 额度守卫   [已完成]
│   ├── metrics.py        # 表1/2/3 绩效指标                        [已完成]
│   ├── universe.py       # 点对点可投资股票池                       [已完成]
│   ├── backtest.py       # 持仓构造 + 净值引擎                      [已完成]
│   ├── analytics.py      # 图6/8–28 的统计量，纯计算               [已完成]
│   └── plots.py          # 绘图（6 种形状 + 中文字体）              [已完成]
├── scripts/
│   ├── 01_fetch.py       # 一次性抓取，可续传，13 步                [已完成]
│   ├── 02_backtest.py    # 表1/表3/图7/图30 + Q14 敏感性           [已完成]
│   ├── 03_analytics.py   # 图6、8–28、31                            [已完成]
│   ├── 04_signal.py      # 当期 100 只名单                          [待做]
│   └── 05_oos.py         # 样本外回测（延伸至 2026）                 [已完成]
├── tests/                # 174 个测试，1 秒跑完，不耗额度
│   ├── test_metrics.py   #   公式 ←→ 研报表1/表3 发布值
│   ├── test_benchmark.py #   本地指数数据 ←→ 表1/表2 基准行（含 9 个带日期的回撤）
│   ├── test_universe.py  #   谓词逻辑（合成数据）+ 两条真实数据不变量
│   ├── test_backtest.py  #   净值引擎，期望值全部可手算
│   └── test_analytics.py #   诊断统计，期望值全部可手算
├── data/                 # 705 MB Parquet 缓存 — 已 gitignore，⚠️ 需在 git 之外单独备份
└── output/               # 净值、表格、敏感性、诊断 CSV
    └── figures/          # 23 张 PNG（图6、8–28、31）
```

### 现有程序做什么

**`smallcap/data.py`** — 唯一与 rqdatac 通信的模块。提供可续传的分块抓取（`harvest`）、原子写入的 Parquet 缓存（`write`/`load`/`wide`）、以及宁可抛 `QuotaExhausted` 也不静默烧完额度的守卫。其中固化了实测的成本模型：约 4~8.5 B/field-cell、**每次调用约 250 KB 固定开销**、额度计量在调用后约 5 秒结算。固定开销是"一切都用少量大调用抓取"的原因——逐股循环会把整个额度烧在调用开销上。

**`smallcap/metrics.py`** — 年化收益/波动、带峰谷日期的最大回撤、夏普、卡玛、信息比率，以及完整的表 1 版式（策略/基准/超额 × 分年度 + 全时段）。公式由研报公布的数字反推得到。**两条口径靠研报自己的基准行钉死**（见 grill.md Q16/Q17）：年化收益按**自然日**折算而非 252 交易日；分年度的**收益**从上年末起算、**风险**只算年内。注意研报对"超额"的口径也自相矛盾：超额**收益**是年化算术差，而超额**回撤**用的是比值净值——均按原样复现。

**`smallcap/universe.py`** — 谓词函数列表构成的两级过滤：常设池（有价、非 ST、上市满 20 交易日）与调仓日附加（非涨停、非停牌）。加变体就是往列表里追加一个函数。另有 `smallest(n, skip=)` 出市值分档、`deciles()` 出十分组。北交所过滤**没有实现**——数据里根本不存在北交所标的，该前提由测试断言。

**`smallcap/backtest.py`** — 日度净值引擎。等权、期间自然漂移、按 `sum|Δw| × 单边费率` 计费。**成交口径只有一种**：T 日收盘出信号、T+1 开盘成交（grill.md Q19），实现上把成交日的收益拆成隔夜腿与盘中腿，旧权重吃隔夜、新权重吃盘中，非成交日走收盘到收盘。`trade_dates()` 是信号日→成交日的映射，分析层也要用它对齐。返回 `Result`，含逐期目标权重、漂移后权重、换手率与费用（**按成交日索引**）——诊断图要的东西都在里面，不必重跑。另有 `run_with_stops()` 叠加止盈/止损（图31）：同一条 T+1 开盘口径，但引入路径依赖的日度状态机（逐票跟踪建仓参照价），触发即卖、所得**等额**均分给剩余持仓以保持满仓；两条阈值都设 `inf` 时逐点退回 `run()`。

**`scripts/01_fetch.py`** — 抓取脚本，13 步。`--smoke` 只花几 MB 校验字段名/形状/dtype 并给出预算推算；不带参数则真正执行，按价值排序，中途失败也能留下一份可编码的数据集。三个关键辅助函数：`block_ids()`（对区间取**并集**，而非取块末名单——后者会漏掉块内退市的股票）、`melt_flags()`（宽表布尔 → 仅 True 的事件表）、`turnover()`（`get_turnover_rate` 的索引名是 `tradedate`，且 `get_factor` 取 `turnover_rate` 会静默返回全 NaN）。

**`scripts/02_backtest.py`** — 验收脚本。先打印全部先验假设，再跑表1 对照、六项结构性检查；`--full` 追加频率对比（图7）、市值分档（表3/图30）与 **Q14 单变量敏感性映射表**。全程离线。

**`smallcap/analytics.py`** — 研报 4.1、4.3–4.9 节全部诊断量的纯计算层，不画图。三条贯穿的约定：①**成员一律用布尔宽表**，指数成分先转成与组合持仓同型，两边才走得上同一条代码路径——图19/20–24 的对比要有意义，算法必须逐字相同；②在调仓日上算的（基尼、分位数漂移、调出归因）取 `selection`，在每个交易日上算的（PB、市值中位数、换手率）取 `daily(selection)`；③**缺数据就让它缺**，中证1000 成分股在 2014-10-17 之前直接没有行，图上是缺口不是前值。`exit_reasons` 值得单说：它**不另起一套判据**，而是逐条问「这只股票在调仓日是哪条谓词没通过」，所以分类天然穷尽且互斥。

**`smallcap/plots.py`** — 只放**图形**不放**图号**：研报 31 张图归根到底只有 6 种形状（多线、密度、横条、分组柱、堆叠柱+右轴线、双轴），按形状写函数不必写 31 遍几乎一样的代码；哪张图用哪种形状写在 `03_analytics.py` 里，挨着算它的那几行。`use_font()` 按可得性挑中文字体，一个都没有就 warn 而不是静默画出满屏方框。

**`scripts/03_analytics.py`** — 分析层主程序，图6、8–28、31。每一节都把研报的定性结论与本文实测并排打出来，对不上就直说对不上（图16、图24 各有一处，见 grill.md）。**图31 止盈/止损**跑 `bt.run_with_stops` 的三条曲线并对研报的三条定性结论做结构性检查（研报只给定性、无数字可对）。`--deciles` 追加图6 十分组（2007–2021，约 1 分钟）。全程离线。

**`scripts/05_oos.py`** — 样本外回测，把 02_backtest.py 的策略参数**逐字不变**只把区间延到 2026-08-07（论文回答不了的部分）。先跑样本内 sanity（切片仍应复现 43.00%，证明 `_ext` 数据未污染样本内），再报样本外块指标（baseline / 持有停牌 / OOS-only 三口径交叉核对）、逐年、**2024-01 微盘踩踏 vs 2015-06 股灾**的拆解与回本时间、以及**调出归因迁移**（退市新规前后）。产出 `output/figures/oos_nav.png`、`oos_crash_2024_vs_2015.png` 与 `output/oos_*.csv`。全程离线。**结论写在 grill.md「样本外回测」，本脚本是它的可复跑凭证。**

**`tests/`** — 187 个测试，1 秒跑完，全部不耗额度。分工：`test_metrics.py` 用表3 的 10 个市值档与表1 的 10 个期间**同时**校验公式；`test_benchmark.py` 拿本地指数数据对研报的基准行（含 9 个带日期的年度回撤），把数据与口径的问题挡在回测之前；`test_universe.py` 用合成数据钉死每条谓词；`test_backtest.py` 与 `test_analytics.py` 的每个期望值都能在纸上手算，不是把当前输出抄回来当基准。这套测试的作用是：若哪天对不上 43.1%，能立刻排除掉"公式/数据/引擎算错了"。诊断统计尤其需要这层保护——**年化对不上研报会立刻暴露，而一张形状大致合理的概率密度图可以错很久都没人察觉。**

---

## 数据清单

`data/`，705 MB，**6,445 个交易日 × 5,543 只股票，2000-01-04 ~ 2026-08-07**。

| 数据集 | 内容 | 用在哪 |
|---|---|---|
| `price_post` / `_back` / `_ext` | 后复权 open + close | 收益、成交、图11–14 股价分位数 |
| `price_raw` / `_back` / `_ext` | 不复权 close、`limit_up`、`volume`、`total_turnover` | 涨停/停牌判定、图24 换手率分母 |
| `market_cap` / `_back` / `_ext` | 总市值 | 排序因子、图10/15–23 |
| `st`、`suspended` | 仅 True 的事件表 | 选股过滤、图26 调出归因 |
| `pb` / `_ext` | `pb_ratio_lyr` | 图19 整体法 PB（净资产 = 市值 / PB） |
| `turnover` / `_ext` | `turnover_rate`（**百分数**） | 图24 换手率的流通A股对照口径 |
| `free_circ` / `_ext` | `free_circulation`（自由流通股本） | 图24 换手率分母（自由流通市值 = 不复权收盘 × 它） |
| `index_components` | 三大指数日度成分 | 图10/15–24 的指数对照侧 |
| `index_weights` | 指数发布成分权重（2022-03-31 快照，自由流通为基） | 图16 ground truth：验证研报用发布权重而非总市值，不进正式绘图 |
| `instruments` | 上市/退市日期 | 推导点对点股票池、退市归因 |
| `calendar`、`index_price`、`industry` | 交易日历、三大指数收盘、中信一级行业（**citics_2019**，年末快照） | 全局 |
| `index_csi2000` | 中证2000 收盘（`932000.INDX`，回算自 2013-12-31）| 样本外副基准（05_oos / 07）|

按时段拆分：`_back` = 2000-01~2012-12，无后缀 = 研报区间，`_ext` = 2022-04~至今。

**已验证**：2013–2022 年间退市的 75 只股票全部在库，且每只的数据恰好终止于其退市日。**无幸存者偏差。**

---

## 待办

### 0. 补齐抓取缺口 —— ✅ 已完成
- [x] `index_components`：三大指数日度成分（日期区间形式，每指数每块 1 次调用）
- [x] `pb_ratio_lyr` + `turnover_rate`：全市场日度面板
- [x] **2007–2012 选股字段**（开工时才发现的缺口）：`market_cap_back` / `price_raw_back` / `price_open_back` + ST/停牌事件表 —— 没有这块**图 6 十分组根本跑不了**
- [ ] 断网复验全流程

### 1. 策略层
- [x] `universe.py` — 过滤链做成谓词函数列表（ST、上市不满 20 日、涨停、停牌；北交所无需实现）
- [x] `backtest.py` — 持仓矩阵 → 日度净值；等权、计费、期间自然漂移
- [x] ~~两种成交模式并行~~ → **已按 Q19 收敛为只有 T+1 开盘**。月频只差 0.09pp（结论保留在 grill.md），但**日频差 13.87pp 且方向相反**，推翻了研报的图7 结论
- [x] 市值分档 100…1000（`smallest(skip=)`），十分组（`deciles()` → 图6 已跑，见分析层）
- [ ] 容量模型：每票每日限当日成交额 5%，未成交顺延次日（表2/图29）
- [x] 止盈 13% / 止损 26% 叠加层，卖出资金均分至剩余持仓（图31）—— `bt.run_with_stops`，三条定性全成立：止盈 **51.3%** 增厚（基准 43.0%）、加止损全时段反而 **−2.9pp**（研报「止损效果一般」）、止损触发 **2015 独大 99 次**（研报「仅 2015 有用」）。**⚠️ +8pp 增厚幅度含微观结构红利**（止盈触发 3468 次＝准高频再平衡，51.3% 与日频 51.9% 同一机制）——方向可信、幅度别全信，见 grill.md「止盈、止损」
- [x] **停牌持仓的处理** —— 引擎加了 `suspended` 开关（持有到复牌，不按冻结价卖出），已做成单变量敏感性项。**实测推翻旧判断**：全时段仅 **−0.29pp**（不是"最大偏差源"），且**两面**——2015 −33.3pp、2014 +8.0pp，几乎抵消。研报自己也带这偏差（2015 +267% 只有 baseline 对得上）。baseline 仍作默认（复现研报）。见 grill.md「停牌持仓的处理」

### 2. 第一个真正的验收点 —— ✅ 通过
- [x] 表 1 → 月频最小 100 跑出 **43.09%**（研报 43.1%）
- [x] **结构性检查全部成立**：分档 43.1%→11.4% 且唯一非单调点与研报同处 800→900；调出 2368 次 vs 2401；2018 年后名义换手率 38.0%；最大回撤 2015-06-12→07-08 逐日吻合
- [x] **数量性检查**（阈值 年化 ±5pp、夏普 ±0.2）：年化 43.09% vs 43.1%、夏普 **1.269** vs 1.28、信息比率 2.644 vs 2.651、卡玛 0.776 vs 0.788
- [x] Q14 单变量敏感性映射表 → `output/sensitivity.csv`
- [x] 调出**原因**归因 → 折叠成研报三类后 **97.85 / 2.15 / 0.00**（研报 97.17/2.75/0.08）

### 3. 分析层 —— ✅ 全部完成
- [x] 图 6 十分组（5.97% → 36.56%）· 图 8/9 基尼（盈利 0.446 > 亏损 0.336）· 图 10 横截面分位数漂移（97.3% 为正）· 图 11–14 建仓/卖出股价分位数（三阶段均值差 +0.14）
- [x] 图 15–25 行业/PB/市值中位数/换手率 · 图 26 调出归因 · 图 27 相关系数（0.938/0.903/0.678）· 图 28 日历效应（**15 个数最大偏差 0.02pp**）
- [x] `plots.py` — 中文字体已显式设置，实测选中 `PingFang SC`
- [x] 图24 换手率水平偏低 —— 已定位为分母口径：实测 rqdatac 换手率分母 = 流通A股（比值 0.997），改用**自由流通市值**分母后 2.55%→**3.66%**（逼近研报目测 4-6%，代价是中证1000 略反超）。见 grill.md「图24 换手率口径」
- [x] 图16 沪深300 银行 —— 已定位并改（两处叠加）：(1) 加权口径 研报注写「总市值」实为自由流通发布权重；(2) 行业分类源 `zx` 异常（把宁德时代错分到汽车）须换 `citics_2019`。两者一改后沪深300 全 28 行业对上研报（最大 0.62pp，发布权重按 citics_2019 汇总逐行一分不差），银行 21.5%→**12.3%**（研报 12.77%）。连带修好图18 中证1000、推翻图15「说得太满」。见 grill.md「两处对不齐」#2

### 4. 样本外（论文回答不了的部分）—— ✅ 已完成（`scripts/05_oos.py`，详见 grill.md「样本外回测」）
- [x] 重跑至 2026 年：样本外块年化 **28.3%**（样本内 43.0%）、超额 +25.3pp、夏普 0.76/IR 1.03（样本内 1.27/2.64，腰斩）
- [x] 拆解 **2024-01-02 ~ 2024-02-08** 并与 2015-06 对照：策略 −44.5% vs 基准 −14.7%（≈3×，**微盘独有流动性螺旋**），2015 是 ≈1.2× 的系统性 beta；2024 无停牌潮、两停牌口径零分叉，这 −50% 比 2015 的 −55% 更「真」
- [x] 退市新规后调出归因迁移：**戴帽翻三倍** 2.2%→6.2%（新规后 7.1%，2025 最密），退市仍恒 0（濒退市股先被戴帽/停牌过滤），停牌 12.2%→1.4%（结构性）
- [x] 中证2000 副基准已补（`index_csi2000`，`932000.INDX`）：05_oos 样本外超额 **+25.3→+22.2** 对中证2000（用中证1000 高估约 3pp 纯度）；2024 窗口中证2000 −27.6% vs 中证1000 −14.7%（微盘专属）
- [ ] 待补：万得微盘基准（rqdatac 取不到）、容量模型（样本外拥挤度更该做）

### 5. 信号
- [ ] `04_signal.py` — 输出当期 100 只名单，附剔除原因便于人工复核

### 杂项
- [x] `.gitignore` —— 已写。忽略 `data/`、逐日序列 CSV、`__pycache__/`、`.pytest_cache/`、`*.tmp`；**刻意保留** `harvest.log`（21 天后就再也拉不出来的数据集，其唯一来源凭证）、汇总表 CSV、`output/figures/` 的 23 张图（约 3.3 MB，代价与理由写在 `.gitignore` 里）、以及图26 的逐笔明细（验收凭证）
- [ ] **`data/` 需在 git 之外单独备份** —— 试用授权到期后 `01_fetch.py` 再也跑不出这份数据

---

## 坑位备忘

- **年化收益按自然日折算，不是 252 交易日。** A 股一年只有约 243 个交易日；用 252 会把中证1000 全时段从 9.7% 抬到 10.10%，把 43.1% 抬到 45.0%。分年度还要再分一层：**收益**从上年末收盘起算，**波动与回撤**只算年内（2016 年熔断那天是判别性证据）。详见 grill.md Q16/Q17。
- **`get_factor(ids, ["turnover_rate"], ...)` 静默返回全 NaN**，不报错。换手率必须走 `get_turnover_rate(..., fields="today")`，而且它的索引名是 `tradedate` 不是 `date`。
- **rqdatac 没有自由流通市值因子**（标定 6 个候选：3 个是总市值、3 个是流通市值，无一是自由流通）。图24 要自由流通口径，分母只能由 `get_shares` 的 `free_circulation × 不复权收盘价` 自己算。另：`get_turnover_rate` 反推出的分母实测是**流通A股**（比值 0.997），不是自由流通。
- **停牌持仓：旧口径按冻结价被"卖掉"，新口径可持有到复牌**（`bt.run(..., suspended=)`，volume==0 即停牌）。现实里停牌股卖不掉。**实测偏差两面且净很小**：全时段 −0.29pp，但 2015 **−33.3pp**（股灾停牌股复牌补跌，旧口径卖在跌前=高估）、2014 **+8.0pp**（重组停牌复牌高开，旧口径卖在涨前=低估）。**不是"最大偏差源"**——那是把频率（288/2368 次调出=12.16%、单次最多 27 只 @2015-08-04）错当成了幅度。研报头条也嵌着这偏差（2015 +267% 只有 baseline 对得上，最大回撤 54.7% 亦然）。baseline 仍作默认复现研报，新口径作敏感性项。见 grill.md「停牌持仓的处理」。
- **信号日 ≠ 成交日，分析层必须自己对齐。** 引擎按 T+1 开盘持仓，`selection` 却是按信号日索引的。组合侧的统计（图8/9、10、19–25）要先用 `bt.trade_dates()` 把 selection 贴到成交日上，否则统计的是一个引擎从未持有过的组合。反过来，图26 调出归因必须用**信号日**——那才是过滤条件被求值的时点。
- **A 股隔夜收益系统性为负**（全市场日均 −0.095%），而**刚被选中的小市值股次日早盘跌得尤其狠**（−0.159% vs 一天后的 −0.123%），一天内衰减掉大半。任何高频调仓的回测里，"在收盘买还是次日开盘买"这个选择都会被放大成巨大的收益差——日频下值 14pp。低频（月/周）下只值 0.09–3.4pp。
- **"因退市而调出"在本文口径下恒为 0，这不是 bug。** 三个良定义口径数的不是一回事：调出当日状态（本文）**0**、曾持有后退市 **14（0.59%）**、研报 **2（0.08%）**。那 14 只无一在退市当天被调出——平均提前 **496 天**就因停牌/戴帽被过滤链赶走。研报的 2 **既非本文「调出当日」口径（0）、也非「后来命运」（14）**，而是更紧的中间口径（更像"退市前后不久调出"，或 Wind 多持有了 2 只到退市）。三口径并排见 `03_analytics.delisting_paths`；**不挑窗口凑 2**（Q14）。
- **画多于 10 条线的图要检查配色回绕。** 图6 是 10 个分组加 1 条基准，配色表只有 10 个颜色时第 1 组会和基准撞成同一个颜色，图上看起来像少画了一条线——不报错，只是错。
- **`selection.reindex(sessions)` 会把布尔宽表退化成 object dtype**（引入 NaN）。下游 `where(members)` 会拿 NaN 当真值用。`analytics.daily()` 中间过一道 float 再比回布尔就是为了这个。
- **`a.daily(selection, [单个日期])` 拿不到持仓。** ffill 需要历史行，只 reindex 到一个日期时前面什么都没有，结果全 False。要先 reindex 到完整 sessions 再取那一行。
- **双边换手率有两种口径**，研报自己前后不一致：图25 的 30–40% 是名义口径（进出只数/组合只数），4.7 节的 2401 次调出折成价值口径是 43%。差额来自"调出的正是涨上去的票，权重高于平均"。
- **研报图7 的注写错了**（抄了图6 的"2007-01-01 至 2021-12-31"），图上实际是 2012-12 至 2022-03。
- **研报图15–18 有两个坑，注和分类各一个。** ①注写「各行业总市值占指数总市值之比」，实际是**自由流通/发布权重**（沪深300 银行 总市值 21.5% vs 自由流通 12.3%，只有后者对上研报 12.77%；大国有行自由流通仅占总市值 4–6% 是主因）。②行业分类源要用 **citics_2019**：本地一度用的 `zx_instrument_industry` 是异常源，把宁德时代等错分到汽车（且非 point-in-time），害得电力设备及新能源/汽车、乃至图18 中证1000 最大行业全错。两者都改后沪深300 全 28 行业对上研报（最大 0.62pp）。`01_fetch` 行业源已换 citics_2019 并重抓、`figures_15_to_18` 已改自由流通（总市值口径保留作诊断打印）。见 grill.md「两处对不齐」#2。
- **`limit_up == 0` 不是脏数据** —— 它表示"无涨跌幅参照"：要么当日停牌（`volume == 0`），要么处于注册制新股上市初期的无涨跌幅窗口。两者都已被选股范围过滤掉。附带好处：`volume == 0` 是一个免费的停牌标记。
- **绝不要在同一次 `get_price` 里混用复权与不复权字段** —— `close` 返回的是复权价而 `prev_close` 不是。收益用后复权，涨停/停牌判定用不复权。
- **不复权 close = 后复权 close ÷ `ex_cum_factor`**（已验证）—— 一整个字段可由一张很小的事件表推导出来。
- **中证 1000 成分股最早只到 2014-10-17。** 指数行情回补到了 2007 年，但成分股没有。研报用的是 Wind（会回补）。**在图上标注缺口，不要伪造。**
- **2015 年的停牌潮** —— 2014–15 年有 145,727 个停牌日，而 2020–21 年只有 9,531 个。股灾期间约半数市场同时停牌，且正好落在研报最好的年份（2015 年 +267%）与最大回撤区间内。研报从未讨论这些持仓当时是否卖得掉，**我们应该讨论**。
- **不得为凑出 43.1% 而调参。** 先验固定假设 → 跑一次 → 如实报告结果 → 之后**一次只动一个变量**，记录是哪个假设、把结果推动了多少。这张映射表才是交付物 —— 见 `grill.md` Q14。
- **2024-01 微盘踩踏是样本内不存在的形态。** 样本外策略 −50%、基准中证1000 仅 −15%（≈3 倍），是微盘独有的流动性螺旋（雪球/DMA 去杠杆、量化集中平仓）；2015 是全市场 beta（策略 −55%、基准 −46%，≈1.2 倍）。判别证据：2024-02 基准 +11.7% 而策略 −8.8%，两者**解耦**（救市买指数成分不买微盘）。且 2024 无停牌潮，baseline 与「持有停牌」两口径**零分叉**——这个 −50% 比 2015 的 −55% 更「真」（2015 那个数嵌着「冻结价卖停牌股」的乐观）。见 grill.md「样本外回测」。
- **样本外只用中证1000 会高估超额纯度——已补中证2000 副基准。** 中证2000（`932000.INDX`）rqdatac 回算到 2013-12-31（2023-08 才发布，前段为回算序列），恰覆盖整个样本外块，已抓入 `index_csi2000`。实测中证2000 样本外 +6.0%/年 > 中证1000（用后者作分母把超额抬高约 3pp）；05_oos 已并排两个分母。万得微盘 rqdatac 取不到，容量模型（拥挤度）仍是后续。
