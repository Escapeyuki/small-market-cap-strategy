# ⚠️ data/ 是 705 MB 的 Parquet 缓存，不进 git。
#
# 但请注意：**它不是普通的可再生缓存。** rqdatac 是有硬期限的试用授权，到期后
# scripts/01_fetch.py 再也跑不出这份数据。缓存里含 2000 年起的后复权价、
# 2007 年起的选股字段，以及已退市股票的完整历史（无幸存者偏差）。
# ⇒ **请在 git 之外单独备份 data/**，git 只是不适合装它，不是它不重要。
data/

# 抓取过程的临时文件（data.write 先写 .tmp 再改名，中途被杀会留下）
*.tmp

# 输出：结论留档，逐日序列不留。
# nav_*.csv 是几千行的日度净值，属于可由 data/ 在 20 秒内离线重算的中间产物；
# 而汇总表很小，又是 data/ 不进 git 之后仓库里唯一的结果证据，所以保留。
output/*
!output/table1_*.csv
!output/sensitivity.csv
!output/bands_annual_return.csv
# 样本外汇总表（05_oos.py）：指标对照、逐年、踩踏窗口、调出归因迁移，各几百字节，
# 是「样本外结论」在仓库里唯一看得见的证据。nav_oos_full.csv 是逐日序列，仍按
# nav_*.csv 的约定忽略（可离线重算）。
!output/oos_*.csv

# 分析层：同一条线划在同一个地方。留下的是结论（每张图一行到几十行的汇总）
# 与图26 的逐笔明细——后者是 Q14 结构性验收「调出归因 97.85/2.15/0.00」的
# 审计凭证，2368 行 98 KB，没有它那句结论就只是个断言。
# 不留的是日度/事件级序列：nav_deciles、entry_exit_percentiles、pb_aggregate、
# turnover_rate_aggregate、rolling_correlation，合计 1.5 MB，跑一遍就有。
!output/exit_reasons_monthly.csv
!output/exit_reasons_detail.csv
!output/gini_monthly.csv
!output/cap_percentile_shift.csv
!output/calendar_effect.csv
!output/turnover_both.csv
!output/deciles_annual_return.csv
!output/industry_*.csv

# 22 张诊断图，3.1 MB。**这是一个有代价的选择**：PNG 是二进制，每次重跑重画都
# 会在 history 里再压一份。留它的理由和留汇总表一样——data/ 不进 git，图就是
# 「复现确实成立」在仓库里唯一看得见的证据。若哪天嫌 history 胖了，删掉下面
# 这两行即可，`python scripts/03_analytics.py --deciles` 一分钟重画一套。
!output/figures/
!output/figures/*.png

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# 编辑器 / 系统
.claude/
.DS_Store

# 刻意**不**忽略的两样东西：
#   harvest.log —— 20 KB，记录了每个块的抓取时间、行数与额度消耗。对于一份
#                  21 天后就再也拉不出来的数据集，这是它唯一的来源凭证，值得留。
#   德邦研报 PDF —— 1.4 MB，是整个复现的规格书。

# 忽略CLAUDE.md
CLAUDE.md

# 忽略 .vscode
.vscode/